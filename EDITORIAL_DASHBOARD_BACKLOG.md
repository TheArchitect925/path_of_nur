# Editorial Dashboard Backlog

- Add per-domain drill-down pages for richer item-level audits once the read-only master dashboard settles in production use.
- Add a safe internal PIN change/reset flow on top of the seeded local PIN store.
- Add runtime localization auditing for ARB key coverage if a reliable in-app asset or generated manifest path is introduced.
- Add export/share support for internal audits so the owner can snapshot readiness before releases.
- Add lightweight widget/provider tests for router gating, PIN verification, session relock, and filter behavior.
- Add score-history snapshots so the owner can see whether pack health is improving over time.
- Add per-domain issue explainers and remediation hints so low scores point directly to the next editorial action.
- Add optional safe metadata bulk actions for pack-level readiness only after widget/provider tests are in place.
- Expand the inline editor to additional supported domains such as ayah actions, recommendation mappings, and selected learning-path content once their data models get the same stable override adapters.
- Add richer diff rendering with per-field highlighting and line-level comparisons for longer editorial content.
- Add a small audit/export view for version history so the owner can review recent editorial changes before releases.
