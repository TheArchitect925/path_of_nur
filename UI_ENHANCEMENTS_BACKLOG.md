# UI Enhancements Backlog

Last updated: 2026-03-16

1. Add a dedicated bottom-nav glass tuning token so transparency, blur, and border contrast can be adjusted without editing the shared theme directly.
2. Evaluate adaptive blur intensity for bright versus dark backgrounds so the bottom-nav glass remains legible without manually retuning opacity.
3. Tune the Home shortcuts dock dismissal behavior with an optional micro-threshold so tiny accidental scrolls do not collapse it too aggressively.
4. Consider session-only memory for the Home shortcuts dock open/closed state so it can default open on first visit without becoming sticky across launches.
5. Add an optional compact layout variant for the Home Salah timings card on very narrow screens so all daily prayers remain readable without wrapping too tall.
6. Consider a subtle location/method subtitle on the Home Salah timings card if users need more context for why times changed.
7. Add an optional long-press or secondary control on the Home Salah timing pills for `missed` and undo states without overcomplicating the default one-tap “offered” action.
8. Consider a lighter success animation on Home prayer completion so marking a prayer offered feels confirmed without adding visual noise.
