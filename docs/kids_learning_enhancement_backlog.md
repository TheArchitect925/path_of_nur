# Kids learning enhancement backlog

- Add the first real journeys under the `kids-learning` island instead of routing children into mixed adult discovery surfaces.
- Decide whether `kids-learning` should always appear for adult profiles or only when family/kids features are enabled.
- Add kids-specific supporting tools and entry points so the island is useful before the full journey set is built out.
