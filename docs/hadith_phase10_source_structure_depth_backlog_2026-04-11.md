# Hadith Source Structure Depth Backlog

Date: 2026-04-11

## High-signal next enhancements

- Add richer canonical book-level metadata for multi-book collections so future browse can distinguish source collection from internal book structure where that distinction is authentic.
- Review the remaining large `Riyad as-Salihin` chapter titles for editorial polish and stronger consistency where importer-shaped labels still feel too literal.
- Add a small source-structure QA report that flags entries falling into `Uncategorized` or `General chapter` so metadata recovery can target those buckets intentionally.
- Consider a dedicated source-detail summary block that shows chapter count, uncovered metadata count, and canonical source provenance for study-oriented users.
- If product wants deeper library parity later, add chapter-level search handoff from source browse into the canonical Hadith search owner without creating a second search path.

## Notes

- This pass intentionally kept runtime ownership, trust gating, and route ownership unchanged.
- Fallback chapters are structural guardrails, not fabricated scholarly metadata; they only keep browse hierarchical where the source metadata is sparse.
