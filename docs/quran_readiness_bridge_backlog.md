# Qur’an Readiness Bridge Backlog

Date: 2026-03-24

Potential follow-up enhancements:

1. Add one bundled recitation-quality snippet-audio pack for the bridge phrases so the page can prefer real recitation audio over the current shared phrase-asset/TTS fallback path everywhere.
2. Add one tiny “Open the full surah” handoff after the bridge snippets are completed, starting with Surah Al-Fatihah, while keeping the bridge itself separate from the full reader UI.
3. Add one shared familiar-word highlight helper so bridge snippets can softly accent known shared words like `رَبِّ` inside the ayah context without introducing a full parser.
4. Add on-device QA coverage for small screens, large text, and screen readers on the new Kids and adult bridge surfaces before expanding the snippet catalog.
5. Expand the bridge only through another tiny curated pack of short real snippets, ideally from Surah Al-Ikhlas or `اقْرَأْ بِاسْمِ رَبِّكَ`, after product QA confirms the current Al-Fatihah-first set lands well.
