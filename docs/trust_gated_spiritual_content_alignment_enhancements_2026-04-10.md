# Trust-Gated Spiritual Content Alignment Enhancements

- Add a second focused test later for the iPhone widget sync service that verifies `dua` payloads still populate after the stricter surface-aware trust gating.
- Consider a tiny shared “public spiritual surface policy” helper if lock screen and StandBy later need a stricter fallback order than home widgets.
- When the next stabilization pass happens, decide whether watch prompts should keep their own compact-surface ranking profile or continue sharing the current service-backed selection context.
- After native widget/device validation closes out, consider exposing the selected dua’s trust/debug reason in a developer-only diagnostics path so future content regressions are easier to audit.
