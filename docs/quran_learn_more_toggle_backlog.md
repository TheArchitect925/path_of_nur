# Qur'an Learn More Toggle Backlog

- Audit the remaining hardcoded Qur'an reader settings labels and move them into localization so the full Surah settings panel is consistent.
- Add a focused widget test that verifies the Learn More section hides and reappears in the Qur'an reader when the setting changes.
- Review whether the non-reference `related` discovery card should eventually share the same Learn More setting or remain independently visible by product decision.
