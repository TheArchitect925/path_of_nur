# Journey Stats Backlog

## Safe next enhancements

- Add a small weekly Qur'an reading chart once the current total/today metric has real-device QA.
- Add a matching weekly Qur'an listening chart once the shared player-bound tracker has real-device QA.
- Decide whether Qur'an reading time should later award XP or Drops, then wire it through the existing ledger-backed reward systems instead of a local shortcut.
- Decide whether Qur'an listening time should later award XP or Drops, then wire it through the existing ledger-backed reward systems instead of a local shortcut.
- Decide whether post-Salah adhkar completions should later feed XP/Drops or a worship-specific streak layer once the completion entry point is fully surfaced and QA-verified.
- Add an optional adhkar breakdown card later so users can split lifetime adhkar totals into dhikr sessions, post-Salah adhkar, and any future tracked adhkar sets without crowding the Growth home surface.
- Add weekly and monthly dhikr time summaries once enough real session-duration data has accumulated after the active-session timing fix.
- Add a future reflection trend view that compares tracked worship/growth time week to week once more trustworthy time-based learning/activity sources exist.
- Expand lesson completion aggregation to include more non-topic learning systems only after their completion boundaries are verified as trustworthy.
- Add widget tests for the Growth home Qur'an tracker card and the Journey Stats metric grid.
- Add export-friendly metric labels if this page later feeds share cards or reports.
