# Search Production Rollout Enhancement Backlog

1. Review `WorldLandingPage` for a possible lessons-only search layer if product wants broader World lesson discovery without adding search to the more reflective Explore and Reflection tabs.
2. Decide whether `HadithLandingPage` should extend query behavior beyond the Themes tab into Collections and Paths, or whether that would add more noise than value.
3. Audit specialized Qur'an discovery pages such as `QuranWordsPage`, `NamesOfAllahPage`, and `QuranSearchPage` to decide whether they should stay domain-specific or adopt more of the shared search-field presentation.
4. Localize the older hardcoded page copy still surrounding several search-enabled Learn pages so the search rollout sits on fully translated scaffolds rather than mixed localized and hardcoded text.
