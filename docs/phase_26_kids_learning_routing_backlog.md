# Phase 26 Follow-Up Backlog

## Recommended Enhancements

- Migrate the remaining kids runtime localization extension strings into full ARB-backed localization so Kids Qur'an, Kids Hadith, and related hubs are not relying on compatibility helpers.
- Add one broader Kids Learning route integrity test that exercises the full landing page island set, including backward navigation from the routed destination pages.
- Consider giving `Kids Story Library` first-class collection routing for more collections beyond `prophets` if the Kids IA keeps leaning on collection-specific discovery.
- Review whether `Kids Games` should eventually include progression-aware recommendation ordering so the first surfaced activity matches the learner's current Arabic, story, or dua context.
- Tighten `Fun Learning` into a more explicit creative/discovery cluster later if additional non-game kids content keeps growing there.

## Notes

- This phase intentionally avoided rebuilding the Kids Learning architecture.
- The priority was routing correctness and truthful surfacing of already-built kids content.
