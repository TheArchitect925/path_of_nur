## Qur’an Verse Global Standardization Enhancement Backlog

- Audit remaining reader-adjacent specialized verse surfaces on watch and tvOS and decide which should adopt the shared `QuranVerseContent` widget pattern versus remain platform-specific.
- Migrate older manually seeded Home verse data toward explicit `QuranQuoteRef` ownership so fallback strings can be retired once the curated list is fully verified.
- Replace remaining hardcoded Qur’an-reader settings labels such as `Text Options` and `Audio Settings` with localized keys in a focused follow-up pass.
- Review whether Creation and Celestial reflection cards should later open directly into the Qur’an reader on verse tap for stronger source continuity.
