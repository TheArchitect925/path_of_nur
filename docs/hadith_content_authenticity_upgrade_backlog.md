# Hadith Content Authenticity Upgrade Backlog

- Add a verified import pipeline for trusted Hadith sources so Arabic matn and transliteration can be filled from source data instead of local manual curation.
- Track per-entry verification coverage for Arabic matn, transliteration, and translation so content gaps can be surfaced in admin tooling instead of only user-facing placeholders.
- Expand the shared Hadith content block onto any future full-length Hadith preview surfaces and keep summary cards compact.
- Review whether a dedicated source badge should distinguish combined references like `Bukhari / Muslim` from single-collection citations more clearly.
