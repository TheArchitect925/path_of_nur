# Midnight Manuscript Naming + Settings Backlog

Date: 2026-03-31

## Recommended follow-ups

1. Run a device-level large-text pass on the appearance picker to confirm the new helper lines still feel calm at accessibility text scales.
2. Consider translating the new theme helper copy in priority non-English locales instead of continuing the current English-first fallback pattern.
3. If the appearance page gets a broader copy pass later, lightly normalize the other appearance helper labels so the section reads as consistently premium from top to bottom.

## Notes

- This pass intentionally avoided changing theme behavior or persistence logic.
- The work stayed inside the existing settings-owned theme picker architecture.
