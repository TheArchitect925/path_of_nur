# Hadith Search Surface Alignment Backlog

1. Add a compact search-intent chip row on the Hadith search empty state for quick jumps into `Themes`, `Sources`, `Narrators`, and `Grades`.
2. Add a short “searches translation, Arabic, source, narrator, and grade” helper line under the Hadith search field if users need more confidence about scope.
3. Evaluate whether the Hadith browse page should also launch into Hadith search from a shared discovery field instead of a separate action button for parity.
