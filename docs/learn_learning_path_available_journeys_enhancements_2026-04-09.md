# Learn Learning Path Available Journeys Enhancements

Date: 2026-04-09

## Follow-up options

- Decide whether the older level-based `Available paths` section should later merge with the restored guided-path cards into one clearer IA instead of keeping both visible.
- Add a short section subtitle if the difference between path-level journeys and guided-path starter cards feels unclear in QA.
- Consider surfacing per-card next-step titles for guided paths so the restored cards feel even closer to the older `Start a Journey` behavior.
