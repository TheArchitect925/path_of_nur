# Qur'an Focus Recitation Mode Backlog

1. Add a tiny calm fade-only ayah transition after real-device QA confirms it does not make sync feel delayed.
2. Add a keep-screen-awake option scoped to Focus Mode instead of changing global playback behavior.
3. Add optional open-full-player / open-reader handoffs inside Focus Mode only if users need more transport depth.
4. Add landscape and tablet spacing polish after phone-first QA lands.
5. Consider a dedicated sleep timer only after Focus Mode usage and background playback behavior are stable.
6. Consider a memorization-focused entry into Focus Mode later, but keep it on the canonical player runtime and do not fork the surface.
