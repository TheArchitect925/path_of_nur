# Qur'an Hub Discoverability Backlog

Last updated: 2026-03-23

## Enhancement options

1. Add one calmer bridge from `/quran` into `/quran/learning` that explains the difference between reader-first use and study-first use for new users.
2. Decide whether `/quran` should also surface `Qur’an Topics` via a richer featured card once that page is fully localized and no longer partly hardcoded.
3. Review whether `Qur’an Topics` needs its own subtitle/localization pass before it becomes a more prominent discovery surface.
4. Consider exposing `QuranReflections` more strongly as a “Saved study” area if product wants the main hub to support repeat study more directly.
5. Audit whether `quranTopicExplorer`, `quranTopWords`, and `quranWordReview` should also appear in Home global search suggestions more aggressively.
