# Home Prayer Forbidden Overflow Fix Backlog

- Verify the Home prayer summary at the smallest supported phone widths with longer localized forbidden-period labels.
- Consider using the same responsive stat-row treatment on other compact Home summary rows if future copy becomes longer.
- Add a small golden or widget test for the forbidden-prayer summary state to catch future row overflows.
