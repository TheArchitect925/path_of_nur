# Startup Loading Backlog

## Natural enhancements
- Replace the lightweight staged status sequence with real startup/bootstrap checkpoints if broader restore orchestration becomes explicit later.
- Add the optional night greeting variant for late-night launches after product review.
- If the loading screen keeps Arabic devotional copy, continue sourcing it from authenticated adhkar or other trusted references rather than product-written Arabic phrases.
- Fine-tune the transition timing between native splash and in-app loading to eliminate any device-specific flash on slower phones.
- Consider a subtle fade-out transition when leaving `/startup` once device QA confirms it feels smoother than the direct route swap.
