# Kids Dua Audio + Read-Along Backlog

## Next sensible enhancements

- Add real bundled MP3 assets for the V1 priority duas under `assets/audio/kids_dua/en/path_of_nur/`.
- Author segment timing offsets or per-segment audio assets so tap-to-repeat can play exact line clips instead of falling back to whole-dua replay.
- Add learner-scoped widget tests for the lesson page modes, bedtime-link card, and missing-audio states.
- Extend learner scoping to kids dua creative/drawing state if those surfaces become child-profile visible in Family Mode.
- Add a lightweight kids dua recommendation surface on the landing page for `Continue practicing` and `Tonight's dua`.
- Add optional narrator/language preference selection once the broader narration pipeline exposes multiple kids-dua audio variants.
- Surface learner-scoped kids dua activity summaries in the parent dashboard after a dedicated per-learner dua activity log exists.
- Add a small internal asset validation helper for missing kids-dua audio files, similar to the bedtime-story media checks.
