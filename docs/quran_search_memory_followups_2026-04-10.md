# Qur'an Search Memory Follow-ups

- Add an optional settings toggle to disable local Qur'an search history if product wants explicit privacy controls beyond manual clear.
- Consider scope-aware recents so reader `Current Surah` and `Whole Qur'an` can optionally show smarter reuse without fragmenting the canonical store.
- Add light saved-search badges or field hints in `/quran/search` if users save the same query under multiple field filters.
- Evaluate whether compact surfaces should surface at most one saved search row in the future, or keep them suggestions/recents only.
- If future sync is added, keep it opt-in and separate from the local-first storage contract used in this pass.
