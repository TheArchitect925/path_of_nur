# Learning Category Cleanup Enhancement Backlog

- Add direct deep-link aliases for former `?sub=` Learn category URLs so old saved links can land on the exact subcategory destination instead of the broader filtered Explore view.
- Audit whether `Arabic & Language` should eventually have its own dedicated landing page instead of routing directly into Qur'anic Arabic.
- Consider removing the legacy `learnHubCategory` route entirely after one release cycle if analytics show no remaining compatibility usage.
