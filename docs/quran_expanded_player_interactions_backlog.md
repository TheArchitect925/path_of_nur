# Qur'an Expanded Player Interaction Backlog

Last updated: 2026-03-24

1. Add a dedicated widget test for follow-mode toggling from the expanded player so the full-screen control surface stays aligned with reader settings.
2. Consider a softer drag-dismiss threshold with partial visual feedback if real-device QA shows the current top-swipe collapse is too eager.
3. Add a focused accessibility pass for the expanded player handle and close affordances, especially VoiceOver rotor order and larger text sizes.
