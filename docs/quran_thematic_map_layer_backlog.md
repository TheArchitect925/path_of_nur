# Qur'an Thematic Map Layer Backlog

Date: 2026-03-23

Enhancement options for follow-up review:

- Expand curated theme coverage only through explicit high-signal mappings, not keyword fan-out.
- Add a few more semantically strong starter themes if product wants more breadth:
  - sincerity
  - hypocrisy
  - forgiveness
  - speech
- Add reader-level category cues for `Hadith`, `Character`, `Seerah`, and `Signs` only if user testing shows the current chip-only treatment is too quiet.
- Add a lightweight featured-theme card on `/quran` or `/quran/learning` if the existing `Qur’an Topics` entry still feels too secondary after QA.
- Deepen theme detail pages with one short “why this theme matters” reflection block if the current overview feels too sparse.
- Add more theme-aware route targeting where better owners exist, especially narrower Ayah Insights domain/path handoffs.
- Extend multilingual payoff later by replacing the new non-English English-fallback theme keys with real translations.
- Add more no-link regression tests so weak theme matches do not reappear through future reference-graph edits.
