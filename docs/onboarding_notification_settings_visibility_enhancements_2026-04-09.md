# Onboarding Notification Settings Visibility Enhancements

- Add a focused onboarding-to-settings integration test that confirms `all prayers off` produces `Off` across every prayer reminder tile, including Tahajjud.
- Consider disabling the master prayer-reminders toggle automatically when every prayer mode is `Off` from Settings edits too, not just from onboarding completion.
- Review whether the Settings subtitle count should explicitly mention Tahajjud reminders when enabled so users understand it is part of their reminder setup.
