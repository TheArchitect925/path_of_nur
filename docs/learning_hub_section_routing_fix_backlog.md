# Learning Hub Section Routing Fix Backlog

## Follow-up options

- Audit whether `quranHadith` should eventually land on a dedicated combined section surface instead of the current primary `quranLearningHub` owner.
- Add a focused navigation test for `LearningSectionLandingPage` category cards so future taxonomy edits cannot silently collapse routes back to `learnExploreAllKnowledge`.
- Revisit the long-term role of `/learn/category/:categoryId` now that category cards route directly to their primary destination owners.
