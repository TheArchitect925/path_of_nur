# Learning Path V2 Snapshot Backlog

Date: 2026-03-23

## Enhancement options

- Add a small repo script that snapshots a named feature area into `/Users/shahabmansoor/Developer/Path of Nur Deleted and Cleaned Items/` with preserved relative paths.
- Add a restore script that can selectively copy a snapshot back into the repo after showing a diff preview first.
- Add an optional tarball export beside the raw copied snapshot so one-click off-repo backup is easier.
- Add a generated manifest with file hashes for each snapshot to make later integrity checks easier.
- Add a `feature snapshot` checklist doc so future rollback points are created consistently before multi-phase routing/content passes.
