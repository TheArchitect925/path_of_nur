# Qur'an Reference Detail Sheet Backlog

Date: 2026-03-23

Enhancement options for follow-up review:

- Add theme-chip explanation sheets only if product wants theme taps to pause before opening the full topic detail page.
- Add one short owner-specific preview sentence for stronger routes such as Seerah companion, Character companion, and Divine Life detail pages.
- Decide whether stacked bottom sheets inside the reference viewer should later transition to a sheet replacement flow instead of opening on top.
- Add category icons to the detail sheet if QA shows the current chip-only classification is too subtle.
- Expand explicit curated `relationReason` coverage in the reference graph so fewer links rely on category-level fallback wording.
- Add one more widget test covering Qur’an reference chip taps from the top-level reader `Learn More` section, not only the ayah card chips.
