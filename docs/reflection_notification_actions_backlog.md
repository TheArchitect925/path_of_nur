# Reflection Notification Actions Backlog

- Consider a second optional snooze action such as `Remind me in 30 min` if product wants a broader reflection window without cluttering the default notification.
- If the reflection page gains explicit draft/day query state later, pass that context through the notification payload so the deep link can open directly into the intended daily entry state.
- Add device-level interaction QA for Android and iOS action handling to verify platform-specific dismissal behavior and foreground launch feel.
