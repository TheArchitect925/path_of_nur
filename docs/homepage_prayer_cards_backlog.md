# Homepage Prayer Cards Backlog

- [ ] Replace the temporary prayer-card localization shim with generated localization access once the broader l10n drift is reconciled.
- [ ] Add a focused widget-level regression harness for the completed-home-prayer card without pulling in unrelated Learn/Qur'an compilation dependencies.
- [ ] Consider a dedicated prayer detail sheet/widget file so homepage prayer interactions can be tested in isolation.
- [ ] Explore whether post-salah dhikr should evolve from a single linked 100-count session into a first-class structured bundle model if the Dhikr architecture gains safe grouped-session support.
- [ ] Add a parenthetical helper in the full tracker clarifying that mosque prayer also counts as congregation if product language would benefit from it.
