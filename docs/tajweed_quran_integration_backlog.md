# Tajweed Qur’an Integration Backlog

Last updated: 2026-03-24

## Enhancement options

- Add one more beginner-safe bridge snippet that naturally contains `ghunnah` so the light Tajweed hint layer can connect to the existing adult `ghunnah_intro` lesson with the same calm pattern used for `qalqalah`.
- Add one more beginner-safe bridge snippet that naturally contains `noon saakin` so the hint layer can cover the third existing Tajweed basics lesson without inventing synthetic examples.
- Add a small widget regression test that verifies tapping a linked adult bridge hint opens the intended Tajweed lesson page.
- Add a parallel Kids regression test that verifies linked hint chips route into the existing `tajweed-basics` journey stage instead of the adult Qur’anic Arabic surface.
- Run on-device QA for the new hint cards and highlighted snippet text on small screens, large text, and screen readers to confirm the extra guidance stays calm and readable.
