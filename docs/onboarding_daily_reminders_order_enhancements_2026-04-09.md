# Onboarding Daily Reminder Order Enhancements

- Add a small section label above the two daily reminder toggles so they read as a distinct routine-reminders group.
- Consider visually separating the master `Turn off all notifications` toggle from the daily reminder toggles to reduce accidental broad changes.
- Add a widget test that locks the reminders-step control order so future cleanup passes do not push these toggles back down the page.
