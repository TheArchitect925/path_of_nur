# Foundations Learning Enhancement Backlog

- Add a direct “Who Is Allah?” quick-entry card on the Foundations island so the Tawheed ayah block can lead immediately into the first stage.
- Consider a kids-specific supporting badge or microcopy on the Foundations island when `activeKidsUiThemeProvider` is enabled, so families can tell at a glance that the page is in the child-friendly mode.
- Audit the other journey islands for similar Qur'anic anchor blocks where a short sourced ayah would clarify the section theme without adding extra navigation layers.
