# Kids Arabic Parent Overview Backlog

Date: 2026-03-24

Enhancement options saved from Phase 47:

1. Add one small widget test that scrolls the parent overview page and verifies the weekly consistency row stays visible and localized on smaller devices.
2. Decide whether the parent overview should later expose a quiet deep link into the progress map for “See completed letters” without adding a third competing CTA.
3. If family QA asks for more clarity, add one optional “recent word or phrase” label beneath recent activity while still keeping the card count and density low.
4. Decide whether the home-page family summary card should later mirror one line from the new parent overview, or stay intentionally lighter and keep the detailed view inside the parent dashboard only.
5. If Kids Arabic gains more content breadth, consider a tiny “current pack” label in the parent overview derived from the shared lesson-pack system rather than creating separate parent grouping logic.
