# Main Page Shortcut Stacks Backlog

Date: 2026-04-05

## Enhancement Options

1. Add optional lightweight entrance animation for stack expansion once runtime stability is validated on all main pages.
2. Add page-aware analytics for shortcut opens and per-destination tap frequency so future curation is based on usage.
3. Add optional badge/supporting-state variant for future use cases like unread reminders or resumeable study tools.
4. Consider a settings-owned variant later if Settings gets promoted into a true shell-level destination or utility launcher.
5. Add focused widget tests for shared stack rendering and destination config integrity once the current layout direction is approved.
