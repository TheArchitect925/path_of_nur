# Shortcuts Pill Standardization Backlog

## Follow-up options

- Add optional per-section shortcut palettes to `SectionShortcutAction` if some top-level pages need stronger icon-color differentiation while staying inside the shared dock system.
- Audit non-dock quick-action clusters such as Assistant and Salah page action rows for possible future migration into the shared shortcut family if product wants stricter app-wide parity.
- Add a compact widget test for `ShortcutDock` expand/collapse rendering so future page migrations do not reintroduce one-off shortcut shells.
