# Arabic Learning Search / Filter Backlog

Date: 2026-03-24
Phase: 41

## Enhancement options

1. Add one small “recent searches” strip for adult Arabic only if product QA shows repeat lookups are common enough to justify extra UI.
2. Add one letter-group shortcut row for Kids search if family QA shows children tap filters more than typing.
3. Surface one compact “open all words” or “open all phrases” CTA from no-result states if search analytics later show users often search too narrowly.
4. Consider indexing adult lesson/module titles into the shared Arabic discovery layer after product QA confirms that one search field should replace the remaining adult module-only browsing assumptions.
5. Add one tiny progress-aware result badge such as “new”, “heard”, or “review” only if QA shows the current type labels are not enough to guide people.
6. Decide whether Qur’an Readiness snippets should later expose surah-name aliases and ayah-number tokens more prominently in ranking, especially for adult users searching by `fatihah 1:2`.
7. Consider a dedicated shared search-result widget test for Kids and adult pages if future UX passes add richer result cards or result badges.
