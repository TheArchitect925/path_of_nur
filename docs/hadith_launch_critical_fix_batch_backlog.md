# Hadith Launch-Critical Fix Batch Backlog

1. Resolve the remaining unverified entry `sent_to_perfect_character` with a trusted source-backed Arabic matn and translation reference, or replace it with a verified launch-scope hadith if no canonical source page can be confirmed.
2. Decide whether launch requires a verified transliteration source pipeline or whether release posture should explicitly allow transliteration to remain unavailable until trustworthy coverage exists.
3. Add a lightweight Hadith coverage check so source verification regressions are caught before release.
4. Review whether `HadithContentBlock` should use a dedicated Hadith Arabic style instead of the current Qur'an-adjacent typography once launch blockers are closed.
