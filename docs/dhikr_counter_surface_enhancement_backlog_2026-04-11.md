# Dhikr Counter Surface Enhancement Backlog

Date: 2026-04-11

## Next Enhancement Options

1. Add a very soft completion ring sweep when the session target is reached, while still respecting reduced-motion settings.
2. Persist a user preference for stronger or lighter dhikr counter haptics using the existing onboarding/profile preference model.
3. Add an optional larger kids-mode counter variant with more playful spacing and simpler action labels while keeping the same interaction model.
