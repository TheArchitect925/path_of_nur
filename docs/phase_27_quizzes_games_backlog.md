# Phase 27 Quizzes & Games Backlog

- Migrate the new Quizzes & Games labels from runtime compatibility extensions into full ARB-backed localization resources.
- Add a dedicated widget test for the full in-page games hub once the family-learning profile harness is stabilized for adult-mode discovery surfaces.
- Consider replacing the remaining compatibility `/learn/games/:sectionId` route with a redirect into filtered in-page state after downstream deep links are audited.
- Expand search metadata for games with richer tags and difficulty/theme facets, reusing the shared Learn discovery indexing model.
- Review whether `Review Mode` and `Trivia Challenge` should graduate from filtered `learnQuizzesHub` entry points into more explicit dedicated destinations later.
