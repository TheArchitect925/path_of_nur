# Qibla Finder Site Distances Follow-up

- Consider sorting the sacred-site list by nearest first instead of keeping the fixed curated order.
- Consider adding a lightweight toggle for more sites such as Jannat al-Baqi and Mount Arafat if product wants a richer pilgrimage-oriented view.
- Consider adding a small note that distances are approximate straight-line values from the current location.
