# Growth Progress Card Glitch Backlog

Date: 2026-04-08

## Enhancement Options

- Audit other glass cards that still embed `AppLayeredGlassPillButton` inside `PremiumCard` to remove the last remaining glass-on-glass CTA stacks.
- If the progress section still feels visually noisy on device, consider giving the `LinearProgressIndicator` rows a slightly calmer custom track/fill treatment that matches the newer stable glass system.
