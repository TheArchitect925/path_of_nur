# Dua Entrypoint Cleanup Backlog

Date: 2026-03-31

## Follow-up options

1. Decide later whether `/worship/duas` should remain a stable companion route or redirect to `/learn/duas`.
2. Add a focused route-level regression test confirming both routes continue to instantiate the same shared `DuaHubPage`.
