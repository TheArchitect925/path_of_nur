# Quran Focus Recitation Bottom Nav Fix Backlog

Date: 2026-03-31

## Follow-up options

1. Add a focused layout/widget test proving the shell bottom bar is hidden while `/quran/focus-recitation` is active.
2. Review whether any other full-screen shell routes should also suppress the shared bottom bar instead of fighting it with local padding.
