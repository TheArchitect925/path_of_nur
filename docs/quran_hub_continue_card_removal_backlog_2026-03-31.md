# Quran Hub Continue Card Removal Backlog

Date: 2026-03-31

## Completed in this pass

- Removed the `Continue learning` action card from the `Read Qur'an` section on the Qur'an app hub.
- Simplified the read-actions section so it now shows only the main `Read Qur'an` entry.
- Removed the now-unused continue-reading summary dependency from the page build.

## Enhancement options

- If resume access is still useful, move it into a smaller secondary chip or reader-only entry point instead of a full-width hero card.
- Review the Qur'an hub card hierarchy to keep the first section tightly focused on primary navigation versus in-progress resumption.
- If product wants resume behavior later, consider surfacing it contextually only when the user has recent reading history and only in a lighter-weight format.
