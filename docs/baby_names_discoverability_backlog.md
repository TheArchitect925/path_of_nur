# Baby Names Discoverability Backlog

- Add a widget test covering the `Tools & Explore` category so `Baby Names` remains a first-class Tools entry instead of drifting back under the generic `Journey Tools` owner.
- Consider whether `Baby Names` should later gain a dedicated icon treatment in shared Learn discovery cards for quicker scanning.
- Audit other family-oriented utility surfaces for the same discoverability issue so Tools ownership stays consistent across Learn.
