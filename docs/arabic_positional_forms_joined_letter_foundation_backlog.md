# Arabic Positional Forms + Joined-Letter Foundation Backlog

Date: 2026-03-24
Phase: Shared Arabic positional-form and joined-letter foundation

## Enhancement options

1. Add a small shared `join preview` helper widget so Kids and adult letter-form lessons render positional forms from one presentation component instead of formatting raw strings independently.
2. Extend the shared Arabic foundation with reusable word-shaping helpers that can emit both per-letter joined forms and the full joined-word display for future Kids phrase cards and adult beginner reading helpers.
3. Add one compact adult explanation card for non-forward-joining letters (`alif`, `dal`, `dhal`, `ra`, `zay`, `waw`) so the missing beginning/middle forms are explained directly rather than only shown as unavailable.
4. Add a Kids-safe joined-letter practice card that uses the shared resolver to highlight “this letter holds hands” versus “this letter stands alone after here” without turning the flow into grammar instruction.
5. Add focused widget coverage for the adult letter-shapes lesson and Kids joined-letter chips so future content refactors do not quietly regress the shared positional-form usage.
6. Evaluate whether the shared Arabic foundation should next carry a tiny pronunciation-contrast layer such as soft/heavy or throat-letter hints, but only if a real teaching surface needs it.
