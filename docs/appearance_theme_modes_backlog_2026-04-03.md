# Appearance Theme Modes Backlog — 2026-04-03

## Follow-up options

- Review whether `Noor Kids` should stay in the same adult appearance picker or also be surfaced through the existing kids/family settings flow.
- Decide if the legacy `Default` and `Calm Beautiful` modes should stay fully public long-term or move into an expanded “classic themes” group later.
- Validate that `No Glass` and `No Glass Dark` feel sufficiently distinct from simply toggling `Disable Glass Transparency` on top of other modes.
- Consider adding a small “recommended” badge to `Noor Glass - Default` so the default path is clearer for first-time users.
