# Bedtime Prophet Stories Media Backlog

## Enhancement Options

- Add final cover and backdrop art for all 14 stories in the canonical folders.
- Add a soft illustrated placeholder image so missing cover/backdrop states can use branded artwork instead of icon-only fallback.
- Add Urdu and Arabic narrator manifests once matching TTS exports are available.
- Add optional downloaded-audio support when the app later permits manual media imports or content packs.
- Add scene illustration authoring for bedtime carousel mode, inline story scenes, and quiz companion cards.
- Add a debug-only asset validation page if the content team needs in-app verification instead of test-only checks.
- Add a “play next part” card for the Prophet Muhammad ﷺ multipart sequence once audio assets are live.
- Add child-safe thumbnail variants for the mini player and Tonight card if final artwork density requires dedicated crops.

## Housekeeping

- Decide whether the seed layer should eventually adopt the canonical audio file names directly instead of relying on resolver compatibility paths.
- Decide whether bedtime story artwork should later move to a shared kids media placeholder system if more kids story surfaces adopt art-heavy cards.
