# Qur'an Reader Current-Surah Search Alignment Follow-ups

Date: 2026-04-10

Potential next enhancements after the current-surah/whole-Qur'an reader search alignment pass:

- Add subtle field-match chips to both current-surah and whole-Qur'an result rows if QA shows users want clearer translation/transliteration/Arabic context at a glance.
- Consider reusing the shared highlight-text renderer inside the reader sheet snippets so matched terms are visually emphasized without changing the row structure.
- If current-surah result selection proves strong in QA, consider making the `Search` button label context-aware later, but only after localization review.
- Explore optional scope-aware recent searches so users can quickly rerun a current-surah query or a whole-Qur'an query with clearer intent.
- Evaluate whether current-surah rows should surface localized ayah reference labels in addition to the surah-name title on locales where compact numeric references are more recognizable.
