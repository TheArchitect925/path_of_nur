# Global Transition Settings Enhancements

Date: 2026-04-08

## Follow-up Ideas

- Add a short live preview row in Settings so users can see each transition style before saving it.
- Extend the global transition style to modal sheets only if it does not conflict with platform expectations.
- Add widget tests for `AppPageScaffold` entrance behavior under `default`, `gentleFade`, `noAnimation`, and `reduceMotion`.
- Consider limiting `iOS-style` selection to Apple platforms if cross-platform behavior feels inconsistent in QA.
- Review a small set of heavy horizontally interactive pages to confirm the new global transition behavior stays smooth alongside swipe navigation.
