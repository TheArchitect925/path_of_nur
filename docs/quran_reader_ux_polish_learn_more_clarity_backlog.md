# Qur'an Reader UX Polish Backlog

## Enhancement Options

- Add one optional reader preference for reduced study density if real-world QA shows the study stack still feels heavy on smaller screens.
- Extend the same category-grouped `Learn More` treatment into the surah insight detail flow if users respond well to the ayah-level grouping.
- Review large-text and TalkBack/VoiceOver behavior for chip wrapping and top-card order on smaller devices.
- Consider a slightly richer visual distinction between ayah-local references and broader themes only if QA still reports ambiguity.
