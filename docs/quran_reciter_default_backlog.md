# Qur'an Reciter Default Backlog

Last updated: 2026-03-25

## Enhancement options

- Mark the app-default reciter in the reciter picker for first-time users so the initial choice is clearer without changing saved user preferences later.
- Consider a one-time migration only if product direction explicitly wants existing users with untouched legacy defaults moved to Alafasy as well; this pass intentionally changed only the default for new or invalid settings.
