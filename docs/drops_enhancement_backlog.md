# Drops Enhancement Backlog

1. Migrate the remaining trustworthy direct `oceanDropServiceProvider.awardDrop(...)` call sites to the canonical Drops ledger so Ocean becomes a pure downstream projection instead of mixed ownership.
2. Add a dedicated reusable Drops summary card for Home and Ocean once product decides where total/today drops should be surfaced most prominently.
3. Wire additional Qur'an completion points only where a real completion boundary exists, not from scroll/progress noise.
4. Add a richer Garden gallery detail art system once real assets are ready for `imageAsset` keys.
5. Decide whether Ocean watch summaries should read canonical Drops summary directly or continue mirroring through the Ocean adapter.
