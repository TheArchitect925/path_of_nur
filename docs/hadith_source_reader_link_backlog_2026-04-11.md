# Hadith Source Reader Link Backlog

1. Make the `Reference` tile optionally deep-link into the exact source chapter when normalized chapter metadata is available.
2. Add a compact `Open source lane` action to the reader continuity card when the user enters from a direct open with no lane context.
3. Add source-level save or follow behavior if source collections become a bigger study lane.
