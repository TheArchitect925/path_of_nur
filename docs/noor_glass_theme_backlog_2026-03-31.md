# Noor Glass Enhancement Backlog

Date: 2026-03-31

## Recommended follow-up options

- Run a screenshot/device QA pass for `Noor Glass` on Home, Worship, and Qur'an so the translucency and text contrast can be tuned from real renders rather than token-only review.
- Add a slightly richer preview tile treatment for `Noor Glass` if the current settings mini-card still reads too close to the default light theme in smaller picker layouts.
- Consider an optional `Noor Glass`-aware surface treatment for especially dense nested dashboards if later QA shows the most repeated pills or stat capsules need one more step of translucency normalization.
