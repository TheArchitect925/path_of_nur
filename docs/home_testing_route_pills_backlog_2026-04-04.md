# Home Testing Route Pills Backlog

## Natural enhancements
- Consider gating the Home testing pills behind a debug/dev flag if they should not ship in production builds.
- Consider adding a shared internal QA tools sheet so onboarding, startup, and other route checks live in one consistent place instead of accumulating on Home.
- If more internal test routes are added, consider grouping them under a localized section label rather than leaving only raw action pills.
