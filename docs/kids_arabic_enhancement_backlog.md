# Kids Arabic Enhancement Backlog

## Tracing scale-out follow-up

- Add guided tracing configs for the remaining 23 letters using the same `KidsArabicTracingGuide` structure.
- Tune per-letter thresholds after real child testing rather than applying one global threshold blindly.
- Add multi-stroke letters with stronger segmented guidance where order matters more.
- Add optional hint overlays for tricky letters such as `tha`, `sheen`, `dad`, `ain`, and `ghain`.
- Add bundled offline pronunciation clips for all starter and future letters.
- Add richer success animations only if they stay calm and do not overload kids mode.
- Add widget tests for lesson-page trace feedback and completion-sheet states once the UI stabilizes.
- Translate the new tracing/review feedback keys for the supported non-English locales.
- Consider a parent setting for showing extra trace hints on harder letters.
- Consider a lightweight analytics-free local summary of which trace guides produce the most review-needed letters.

## Daily habit loop follow-ups
- Add a weekly mission layer after the single daily mission stabilizes.
- Add a parent setting for daily mission pacing and grace-day visibility.
- Add sticker bonuses tied to streak milestones once the sticker system is expanded.

## Kids Arabic coloring pages
- Add real print/share support once a printing or share package is approved.
- Add localized viewer help text for additional release locales beyond en/ar/de.
- Replace the starter SVGs with final artist-delivered printable assets when ready.

## Parent shared learning next options
- Add a simple parent lock or gate before opening the parent settings surface.
- Add reviewed parent summary widget tests for Arabic and German RTL layouts.
- Let parent focus expire automatically after one meaningful completion if desired.
- Add a child-safe family celebration sheet after stage-complete and sticker milestones.
- Extend parent support levels into review pacing and hint density.
