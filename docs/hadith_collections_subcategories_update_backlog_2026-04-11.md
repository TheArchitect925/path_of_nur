# Hadith Collections and Subcategories Update Backlog

1. Add a small internal audit script that compares runtime collection IDs and subcategory IDs against seeded definitions before future releases.
2. Review whether `hadith_heart` and `heart_softening` should stay separate collections or be clarified more explicitly in copy.
3. Expand collection descriptions and representative hadith sets further if the landing page starts relying more heavily on curated previews.
