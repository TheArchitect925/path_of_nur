# Search Quality UX Enhancement Backlog

1. Add lightweight keyword metadata to quiz catalog items so search can match themes like mercy, prophets, seerah, and hadith topics without relying only on visible labels.
2. Add localized category-label matching to Dua search so users can find duas by the rendered category name in addition to raw dataset ids.
3. Review specialized domain search fields outside the standardized discovery pass, especially Qur'an reader internals and hadith-specific tools, to decide whether they should adopt the shared discovery search styling or remain intentionally distinct.
4. Consider a small shared debounce helper only if future datasets become large enough for noticeable per-keystroke filtering cost; the current targeted discovery pages do not need it yet.
