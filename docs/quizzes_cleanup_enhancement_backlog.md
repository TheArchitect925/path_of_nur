Quizzes cleanup follow-up ideas

- Collapse `LearnQuizFilter` into route query state only if product later wants shareable filtered quiz views.
- Localize quiz module and group fallback strings inside `LearnQuizzesHubPage` instead of relying on raw internal labels like `Prophets`, `Hadith`, and `Review`.
- If Islamic Trivia should eventually merge into the main quiz hub, handle that as a separate product pass instead of reviving a Quizzes-vs-Challenges selector layer.
