# Onboarding Reminder Master Toggle Cleanup Enhancements

- Add a small integration test that confirms the top `Turn off all notifications` switch sets daily reminders off and every salah reminder to `No notification`.
- Consider disabling the daily reminder toggles visually while the master switch is on so the relationship is clearer to users.
- Review whether the same `all off` behavior should exist in the main Settings reminders screen for consistency beyond onboarding.
