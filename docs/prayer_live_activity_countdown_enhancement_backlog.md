## Prayer Live Activity Countdown Follow-Up

- Run device QA for Dynamic Island and lock screen Live Activity around prayer completion toggles, especially current-prayer to next-prayer switching.
- Verify end-of-day behavior after `Isha` completion and around the `Tahajjud`/next-day `Fajr` boundary on a real device.
- Investigate the existing iOS simulator Runner script-phase build failure so native widget/UI changes can be verified through a full local build without relying on partial logs.
