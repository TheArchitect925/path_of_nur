# Qur'an Learning Path System Backlog

## Enhancement Options

1. Add one optional `Continue your path` card to the main `/quran` hub only when continuity exists, so returning users can resume without opening the full paths browser first.
2. Expand the V1 path catalog with one Surah Yusuf path and one Al-Kahf path, but only through explicit curated steps and canonical study owners.
3. Decide whether one future `Theme Study` path should become parameterized from the thematic map, or whether paths should remain fully curated and fixed for product calmness.
4. Add one lightweight completion marker per step if user testing shows the current continue-state is too implicit.
5. Add one small search/discovery bridge so shared Qur'an search can surface learning paths by title, subtitle, and theme tags if the new owner proves valuable.
6. Decide whether memorization-related paths should eventually branch by ayah-first vs surah-first review style, but only after the current `quran/review` lane is validated in real use.
7. Evaluate whether one Journey-owned lesson should deep-link into a matching Qur'an path rather than a single reader state when the learning intent is broader than one ayah.

## Notes

- Keep paths lightweight and route-backed.
- Do not clone the full Journey architecture inside Qur'an.
- Prefer a smaller number of high-signal curated paths over a large catalogue.
