# Major Page Shortcuts Backlog

- Decide whether the shared major-page shortcut registry should later expose a user-facing show/hide preference in Settings, or remain an always-on navigation affordance.
- Add widget coverage for the new major-page shortcut registry so future route or label changes on Learn, Worship, Growth, and Qur'an hubs do not silently drift.
- Consider whether secondary section pages such as Growth subpages and Qur'an learning subpages should later opt into preset-based shortcuts too, or stay intentionally lighter than the top-level hubs.
- If tvOS mirrored section hubs are added later, reuse the same registry contract instead of reintroducing page-local shortcut lists.
