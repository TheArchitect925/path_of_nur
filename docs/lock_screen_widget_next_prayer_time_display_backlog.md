# Lock Screen Widget Next Prayer Time Display Backlog

## Follow-up options

- Add the scheduled prayer time to the Dynamic Island expanded prayer row for parity with the lock screen card.
- Replace hardcoded prayer widget labels such as `Current Salah` and `Next Salah` with a localized widget string strategy if the iOS extension localization pass is prioritized.
- Validate the updated lock screen layout on the smaller live activity heights used by older iPhone lock screen presentations.
