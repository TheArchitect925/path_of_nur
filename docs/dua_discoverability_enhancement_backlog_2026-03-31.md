# Dua Discoverability Enhancement Backlog

- Add a dedicated result summary row in the Dua hub that shows active category and subcategory filters plus the current verified result count.
- Add optional sort modes for the Dua list such as `core first`, `alphabetical`, and `recently saved` while keeping the current default simple.
- Add synonym metadata for common search intents like `rizq`, `income`, `stress`, `anxiety`, `parents`, and `children` so search can match user language more naturally.
- Add a reusable repository helper for filtered Dua result sets if more Dua surfaces need consistent query behavior later.
- Consider a small “popular subcategories” section on the categories tab once more Sunnah stubs are completed and verified.
- Add lightweight widget tests for category chip filtering, subcategory chip filtering, and category-card subcategory drill-in behavior.
