# Quran Reader Recommendation Loop Backlog

1. Audit whether `kidsReader` should use the same anti-rotation presentation guard as the adult reader.
2. Add lightweight debug logging behind `kDebugMode` for recommendation bundle swaps if the reader still feels unstable in edge cases.
3. Review whether the spiritual moment surface should keep cooldown writes on reader opens or use its own calmer presentation policy.
