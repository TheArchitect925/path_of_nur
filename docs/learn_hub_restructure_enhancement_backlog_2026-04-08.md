# Learn Hub Restructure Enhancement Backlog

Date: 2026-04-08

1. Add real non-English translations for the new Learn hub keys now that the English-first localized structure is in place.
2. Decide whether the new existing-Muslim focus choices should gain a richer persisted model instead of being bridged through the current learning-path level plus secondary exploration state.
3. Add dedicated shared saved-learning destination ownership so the `Saved` utility tile does not need to route through the Qur'an reflections surface.
4. Revisit the remaining legacy Learn tab widgets and remove the old Qur'an-tab assumptions once the last callers are retired.
5. Add focused widget tests for the new Learn landing, kids Learn home, and path picker state transitions.
6. Run a follow-up IA pass on `/learn/legacy` and the remaining compatibility aliases after deep-link usage is confirmed.
