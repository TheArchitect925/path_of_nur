# Global Glass Settings Backlog

Date: 2026-04-07
Scope: Shared content glass surfaces

## Enhancement options

- Audit remaining direct `NoorGlassCard` content containers that bypass `PremiumCard` so they can adopt the shared loading-screen shell where product-appropriate.
- Run visual QA on dense content screens to confirm the new default padding does not create unintended vertical expansion on smaller devices.
- Evaluate whether a dedicated shared "content glass preset" should be introduced later so the app can keep one named recipe without repeating loading-screen values in multiple wrappers.
- Review tvOS mirrored content surfaces in the next parity pass and align any shared content-card wrappers that still drift from the mobile glass direction.
