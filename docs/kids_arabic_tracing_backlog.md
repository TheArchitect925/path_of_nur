# Kids Arabic Tracing Backlog

Last updated: 2026-03-22

## Enhancement options

1. Add the next vector rollout pack for `sheen`, `sad`, `dad`, `taa`, `zaa`, `ain`, `ghain`, `fa`, `qaf`, `waw`, and `ya` so the remaining fallback set keeps shrinking in coherent shape families.
2. Add a parent-facing “recommended next letter” summary card on the Kids Arabic home surface now that lesson progression has explicit next/previous flow.
3. Add a lightweight settings toggle for ghost preview and completion pronunciation if user testing shows some children benefit from a quieter mode.
4. Add a full lesson-page widget test for trace completion, reward handoff, vector-vs-fallback rendering, and next-letter routing across a mid-alphabet transition like `zay -> seen`.
5. Consider adding a small per-letter difficulty tuning table if later curved letters like `ain`, `ghain`, or `ya` need gentler thresholds than the current template defaults.
6. Consider a lightweight lesson-complete confetti or haptic hook that stays calm and uses the ready/completion flow instead of a separate reward system.
7. Add optional calm sticker-style celebration art once the final kids reward language is settled, keeping it separate from XP accounting.
8. Add a parent-facing mastery summary card that highlights completed, practicing, and review-ready letters without requiring parents to open the full progress map.
9. Add a lightweight “practice again tomorrow” cue on the progress map if review-ready letters stay unfinished for multiple days, still without introducing a full spaced-repetition engine.
10. Consider small mastery badges for milestones like `5 letters complete`, `10 letters complete`, and `full alphabet complete`, reusing the existing safe sticker/reward model rather than creating a parallel progression system.
11. Add the next beginner-word pack after `باب`, `نور`, and `قلم`, prioritizing short words that reuse already-learned letters before introducing broader reading content.
12. If the first QA pass goes well, add optional word-level pronunciation autoplay and calmer word-complete art while keeping tracing completion forgiving and non-arcade-like.
13. Consider a lightweight “letters used in this word” deep-link row from the word lesson back to individual letter lessons for children who want to revisit a specific shape.
14. Expand the new reading mode with the next curated word-card pack once `باب`, `نور`, and `قلم` feel stable, keeping the experience recognition-first and calm rather than quiz-like.
15. Add optional phrase-level autoplay and a parent-facing “reading mode today” cue if testing shows families want a faster repeat-listen loop without opening tracing.
16. Consider a small word-to-tracing handoff memory so reading mode can reopen on the child’s last viewed unlocked word without changing XP or completion semantics.
17. Expand the new practice loop with a dedicated word-review recommendation signal if user testing shows completed-word review needs to feel less generic than the current completed-word list.
18. Consider a lightweight “unfinished word” marker if the team later wants explicit resume continuity beyond the current unlocked incomplete-word heuristic.
19. Add an optional parent-facing practice summary card that explains today’s letter, word, and review suggestions without turning Kids Arabic into a stats-heavy dashboard.
20. Add a small parent-controlled Kids Arabic audio settings layer so autoplay, repeat-after-me prompts, and word-card tap-to-hear can be tuned without affecting tracing or reward logic.
21. If families respond well to the new audio layer, add the next pronunciation pack using bundled kid-safe assets or curated recordings while keeping the current TTS mapping as a resilient fallback.
22. Extend the repeat-after-me pattern into the practice loop and mastery map only if user testing shows children benefit from hearing review letters and words before opening the full lesson surfaces.
23. Add a lightweight milestone gallery filter so parents can switch between badges, milestones, and latest celebrations without turning the rewards page into a dense dashboard.
24. Consider a once-per-day “latest achievement” home chip that links into the rewards page only when a new unseen milestone exists, keeping celebration visible without repeating the full reveal card.
25. If product wants slightly richer parent visibility later, add a calm “what unlocked this week” summary on the mastery/progress page using the current derived achievement model rather than a second analytics system.
