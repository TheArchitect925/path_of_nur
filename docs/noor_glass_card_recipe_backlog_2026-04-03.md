# Noor Glass Card Recipe Backlog

Date: 2026-04-03

## Enhancement Options

- Extract the Home Next Salah card into a shared `NoorHeroUtilityCard` recipe/widget so future surfaces can reuse the same layer stack without re-implementing it locally.
- Create a shared visual token set for `outer shell`, `content shell`, `hero panel`, `meta chip`, and `mini stat panel` so future themes can recolor the recipe globally.
- Add recipe docs for other reference surfaces too, especially the shared sacred Qur'an block and the Learn layered pill, so prompt-driven reuse becomes faster and more exact.
- Introduce a small gallery/demo page for internal QA showing the canonical Noor card families side by side: hero utility, standard card, pill, sacred block, and navigation glass.
