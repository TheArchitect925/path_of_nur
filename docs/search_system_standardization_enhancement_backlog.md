# Search System Standardization Enhancement Backlog

- Review whether `/learn/legacy` search and `/learn` journey-first search should converge on one canonical Learn discovery surface.
- Add translated non-English ARB entries for the new shared search hint keys so each locale stops falling back to English.
- Consider standardizing clear-button tooltips and optional helper text across discovery searches if accessibility feedback indicates they are needed.
- Evaluate whether the Qur'an hub should expose grouped inline search results cards instead of simple section filtering once the current IA stabilizes.
