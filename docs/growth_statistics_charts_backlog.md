# Growth Statistics Charts Backlog

- Add an optional report detail route if families want a cleaner full-screen weekly/monthly breakdown instead of share-first cards.
- Consider a toggle between prayer-focused and rewards-focused trend views once QA confirms the current combined trend score is understandable.
- Add a small parent-friendly explainer for how XP, drops, Journey, Ocean, and Garden relate if user testing shows the relationship is still unclear.
- Expand reward-source labeling so source modules map to more polished user-facing labels when the global XP ledger cleanup lands.
- Revisit text-share export once the repo-wide localization generation drift is resolved and image/card snapshot sharing becomes safer to add.
