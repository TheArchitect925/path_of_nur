# Release Bump Codex Ready Backlog

## Follow-Ups

- Archive and sign the new iOS build from Xcode Organizer using version `1.2.13` build `33`.
- Run one final device sanity check on the explicit-engine startup path before external rollout.
- Reconfirm TestFlight metadata and release notes match the startup migration and glass-system cleanup work included in this build.
