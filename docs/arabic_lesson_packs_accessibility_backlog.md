# Arabic Lesson Packs Accessibility Backlog

Date: 2026-03-24

Enhancement options saved from Phase 46:

1. Add one adult widget test for the section page pack block so module-backed pack cards stay visible from the existing adult hub.
2. Decide whether the shared continuity layer should later recommend a pack directly when the next target sits inside a large grouped path, or stay item-first and use packs only for discovery.
3. Consider one optional dedicated pack filter row on adult Arabic only if QA shows the current shared search chips are still too broad once more packs are added.
4. Add pack-aware progress summaries only after pack completion semantics are honest and stable; do not infer misleading “pack completed” counts too early.
5. Decide whether Kids should eventually surface only the top 3 most relevant packs and move the rest behind a lightweight “See more” affordance if the home page grows too long.
6. If future Arabic packs add new curated content types, extend the shared pack model first and keep new entries discoverable through the existing Kids/adult hubs and shared search layer instead of adding isolated routes.
