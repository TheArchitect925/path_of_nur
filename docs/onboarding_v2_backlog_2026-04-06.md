# Onboarding V2 Follow-up Backlog

Date: 2026-04-06

1. Add a true skip-or-finish decision so `Skip` does not simply advance one page while still preserving the current onboarding shell.
2. Move Dhikr feedback preferences into the settings or dhikr-discovery ownership path so users can still tune haptics, sound, and visual response after onboarding.
3. Move family-intro guidance into a calmer settings/discovery surface or post-onboarding prompt instead of first-run setup.
4. Convert the remaining hardcoded transliteration/devotional onboarding strings into localization-backed content where appropriate, while preserving canonical Arabic text.
5. Replace English fallback onboarding copy in non-English ARB files with real translations, especially Arabic and Urdu-facing onboarding surfaces.
6. Consider a small data-driven page registry inside the same onboarding shell later so page order, validation, and progress counts cannot drift out of sync again.
7. Consider reusing the onboarding theme-preview tile as a shared appearance preview component later so Settings and onboarding stop carrying duplicated preview code.
