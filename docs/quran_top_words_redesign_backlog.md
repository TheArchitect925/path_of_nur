# Quran Top Words Redesign Backlog

- Add a focused parsing test for the enriched top-words dataset so future asset refreshes cannot drop Arabic again.
- Review whether learned-state persistence should eventually move from rank-based storage to a stable Arabic word key.
- Add optional target-word highlighting inside example and occurrence ayahs once a safe shared Qur'anic text-highlighting approach is ready.
- If the trusted source begins exposing root metadata reliably, regenerate the local dataset so the new detail page can surface root letters without any UI changes.
- Consider a dedicated occurrence sub-page only if the current bottom-sheet list becomes too large for study use.
