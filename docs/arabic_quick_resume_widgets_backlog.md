# Arabic Quick Resume Widgets Backlog

1. Add a real iOS/Android home-screen widget owner for the new Arabic quick-resume payload so the current `home_widget` bridge can render a production widget instead of only syncing offline-ready data.
2. Add one stable widget-level route test for the shared Arabic quick-resume card on Kids and adult landings once the current shell harness can exercise button taps without extra route boilerplate.
3. Consider a small home-tab shortcut-dock action for adult Arabic and Kids Arabic if product review wants the new quick-resume layer accessible from Home as well as from the Arabic-owned entry pages.
4. Decide whether the adult quick-action set should later expose a direct `Short Surahs` entry after more real-device usage clarifies whether `Continue Arabic` or `Review Arabic` is the stronger long-press action.
5. Run on-device QA for app-icon shortcuts and widget payload refresh after progress changes, locale changes, and offline launches on both iOS and Android.
