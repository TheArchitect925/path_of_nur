# Dua UI Consistency Enhancement Backlog

- Add widget tests for Learn-context vs Worship-context tab visibility so the category selector cannot regress.
- Migrate the seeded dua category labels and selected metadata into localization-backed content so category names and subcategory labels are translated consistently.
- Consider a small reusable tappable premium-card wrapper for other feature hubs that still use button-led cards, so this interaction cleanup can be applied consistently without reimplementing semantics and tap handling.
