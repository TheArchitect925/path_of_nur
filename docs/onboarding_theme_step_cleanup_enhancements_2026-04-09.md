# Onboarding Theme Step Cleanup Enhancements

- Consider reducing the sample preview card height further on smaller phones if user testing still shows the lower preview area folding below the first viewport.
- If the theme step remains one of the longest onboarding screens, consider switching the four theme tiles to a 2x2 fixed grid with a slightly wider preview area below.
- Audit whether the removed explanatory strings on the onboarding theme step should later be retired from localization if they are no longer used anywhere else.
- If needed later, add a tiny one-line helper under the selected theme only instead of repeating full descriptive copy for all choices.
