# Qur'an Daily + Journey Unified Loop Backlog

1. Decide whether the Home daily preview should later show one tiny Journey/theme chip from the unified loop summary, or remain intentionally preview-light until on-device QA confirms the extra context is worth the density.
2. Expand Journey-aligned daily selection only through explicitly mapped stages that already have strong Qur'anic anchors, especially `duas-daily-life`, `seerah-madinah-society`, and future Yusuf / Al-Kahf study journeys.
3. Evaluate whether the unified loop should later prefer one small set of alternate ayahs per mapped Journey stage so the same active Journey does not surface the same daily ayah too often.
4. Decide whether memorized daily ayahs should later show one calmer “reviewed recently” helper line on `/quran/daily`, or stay action-only to avoid over-explaining the loop.
5. Consider one route-safe widget test for `/quran/daily` with an overridden active Journey continue state so the new Journey connection card remains protected against future regressions.
6. Decide whether guided-path continuation should later be merged more visibly into the unified loop when a Journey mapping and a Qur'an path align, or remain a secondary continuation action.
7. Run on-device QA for the updated daily companion order on small screens, large text, and screen readers to confirm the new theme/journey sections stay calm and readable.
