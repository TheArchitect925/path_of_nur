# Learn Hub Backup Restore Enhancements

- Review whether the dedicated `LearnSelfLearningPage` and `LearnKidsPreviewPage` routes should now be retired or kept as secondary entry points after the landing-page rollback.
- If the restored hub remains the preferred direction, prune unused newer Learn-hub localization keys and route affordances in a later cleanup pass.
- Re-run a focused Learn UX audit after the rollback to make sure newer copy changes did not leave mixed terminology between `More to explore`, `Available Journeys`, and other retained Learn surfaces.
