# Homepage Date Tap Calendar Fix Enhancement Backlog

1. Add a localized calendar icon or chevron affordance beside the Home date label so the tap target reads as interactive without changing the layout.
2. Consider a shared date-selection helper test covering Home and Worship so future header refactors do not silently break the tap wiring again.
3. Evaluate whether the Home date tap should later expose the same calendar-mode sheet options as the Worship prayer hub if product wants Hijri-display switching directly from Home.
