# Qur'an Dua Reader Handoff Backlog

Last updated: 2026-03-25

## Enhancement options

- Add a small `Back to Dua` or source-context return action when the reader was opened from a Dua detail page, so users can move back without relying on app navigation history.
- Add a subtle reader badge for focused Dua playback so it is clearer that only the selected ayah or ayah range is looping, not the whole surah.
- Consider a future route-level `selectionLoop` source contract shared across other Qur'an-linked surfaces that may need the same scoped playback behavior without rebuilding this logic again.
