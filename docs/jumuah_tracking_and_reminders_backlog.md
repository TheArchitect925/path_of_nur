# Jumu'ah Tracking and Reminders Backlog

- Review whether Friday prayer notifications should also display `Jumu‘ah` instead of `Dhuhr` in the notification title/body while keeping the internal prayer key mapped to `dhuhr`.
- Audit monthly prayer stats and historical summary surfaces to decide whether any Friday-specific `Jumu‘ah` labeling should appear there too or remain `Dhuhr` for aggregate compatibility.
- Revisit the existing Jumu'ah reminder override settings so the product story between fixed Friday tracker time and optional reminder-time customization is clearer.
