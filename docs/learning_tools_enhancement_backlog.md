# Learning tools enhancement backlog

- Move additional non-journey legacy utilities into the `tools-other` island once each one is reviewed intentionally.
- Decide whether `browse-all` and `legacy-learning` should remain separate islands permanently or collapse later into tools/supporting exploration.
- Decide whether `tools-other` should appear for all profiles or only when secondary exploration is enabled.
- Add a dedicated curated layout for utility-heavy islands if the generic island page becomes too journey-oriented for tools-first content.
