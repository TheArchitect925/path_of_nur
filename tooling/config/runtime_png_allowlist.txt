# Temporary reviewed exceptions for current Flutter runtime PNG assets.
# Runtime imagery should migrate to WebP over time. Keep this list small and
# remove entries as each area completes conversion and reference migration.

# Explicit runtime holdout retained on PNG pending separate visual review.
path:assets/images/backgrounds/Loading.png
