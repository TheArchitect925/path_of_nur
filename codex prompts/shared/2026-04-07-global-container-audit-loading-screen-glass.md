review the full app and find all the containers we are curreently using in all pages and sub pages, we want the global preference to be the following so that everything looks the same across the whole app (the only exceptions here are teh quran quotes, and any pills and buttons within the app) Restyle this section to match the loading screen “Welcome Back” glass card exactly by reusing AppHeroGlassShell with the same padding, tint, alpha, radius, border, and highlight settings.
