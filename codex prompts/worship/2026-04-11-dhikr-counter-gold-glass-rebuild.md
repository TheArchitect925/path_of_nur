# Prompt Archive

on the dhikr page, the tap to count should be larger, instead of showing teh number outside, show how many dhikr have been done for that specific dhikr and what the target is within the round circle itself. Make the interface glass and golden, animate it to golden and add an animation when its clicked along with a short and very subtle vibration use the screenshot as guideline

Follow-up clarification:

leave the remaining dhikr page as is
