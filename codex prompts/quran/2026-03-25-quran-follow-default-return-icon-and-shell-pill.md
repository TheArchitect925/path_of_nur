# Prompt Archive

Date: 2026-03-25

## User Prompt

follow mode should be enabled by default and regardless of user playing ayah from the speaker icon or by pressing on it it should always follow the ayah by default. If user minimizes the player and scrolls around, show a mini icon that would take the user back to the ayah. also instead of showing a mini player, show the user a pill that stays in the middle of the screena bove the homescreen - when clicked user will get the options instead of showing it at all times as a mini player.
