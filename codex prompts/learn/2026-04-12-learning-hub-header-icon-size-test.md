# User Prompt

Icon improvement - lets make the icons (headers) a bit larger, leave the home one as is, and lets start with the learning hub as our test one, we can work on that one first and then once finalized apply. it as a global setting to all other pages
