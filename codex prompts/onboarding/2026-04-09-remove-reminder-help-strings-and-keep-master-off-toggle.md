# Prompt

- [ ] remove the salah reminder styles text and the question mark, remove it from the code too as its not needed. Add another option like the Daily Quran reading reminder at the top and have it off by default, the text for that button should be Turn off all notifications, if the user selects that change all settings with the to off and No notifications on the page and the backend
