Fix teh following for teh settings page:

- [x] Settings Page:


Ok these are the fixes we need to implement:

We are focusing on the settings Page
1. Rename Accounts, Profiles & Sync to Accounts & Profiles
In the Accounts & Profiles Page
- In the top decriiption remove all the text after and device sync. 
- Profile & Personalization -> remove all the text after …profile-related shortcuts
- The Next section shows shows a user icon but it does not match the user icon from the homepage
- The levels and days should match the correct metrics - level should match with growth info - days should show how many days the user has been using the app
- Ramadan, Loss, Gentle Mode should all be under their own collabsile section, not sure myself what to name it, come up with something matching 
- Ramadan mode should enable settings and information about ramadan, with a heavy focus on a UI with Fasting and all
- Loss mode should show guide user through loss by providing them with some hadith, quranic quotes and prayers on death and loss
- Gentle Mode should reduce notifications
- Lets add a Unwell mode - the unwell mode should be for days a user is sick
- Cycle days, for sisters only, should only show up for sisters, is to ensure that they do not loose their streak while being on their period

Appearance is our next page to focus on
- Match the Theme modes based on the ones we have for the onboarding and remove the rest
- Remove the second appearance text as its repetitive 
- Make the cards wider one theme icon per row
- Apply the same configuration to the Choose your team page on the onboarding page (page 4)
- Remove the Disable Glass Transparency
- Transparency slider is not working anymore with the current UI, either lets fix it or not offer that setting
- Make themes mode section collapsible, have it be collapsed by default so users can see the other options at the bottom more easier
- Reset appearance shoudl reset all settings to the default (Noor Glass - Default)
- Add a option, follow day/night mode of phone (and ensure the app checks if the user is using day/night mode)

Salah & Salah Time Settings
- Remove Enable Adhan audio since the user is already selecting if they want audio or not for individual salahs
- Remove the Fajr Adhan option and have the Fajr adhan the one that does not change especially since its lengthier and unique
- Remove Test Fajr Adhan
- The Test Regular Adhan label needs to be changed to Test Adhan
- Test Adhan is currently not running any audio when clicked
- Calendar Display is not showing the text correctly, its 2 letter per line
- Move Jum’uah ovveride to the top

Fix teh following for teh settings page:

learniing hub access, not needed on the settings page
