audit the global noor glass and replace all the functions and settings we set up for it with the following Restyle this section to match the loading screen “Welcome Back” glass card exactly by reusing AppHeroGlassShell with the same padding, tint, alpha, radius, border, and highlight settings.

get me the exact settings we have for the Next salah contianer and its sub containers. Want to use that as the global theme across the app and as a standard for noor glass as that one is not glitching and looks the best

lets do it and make sure we clear out the previous preset
