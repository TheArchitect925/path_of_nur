# Prompt Archive

User request:

`ok lets do taht first instead of dissecting everythign one by one`

Context in thread:

- We had been doing page-by-page Noor Glass cleanup.
- The user asked whether we were also building a global table / matrix so future themes could propagate centrally instead of requiring manual QA on every screen.
- We explained the app was improved but still hybrid, and recommended a dedicated global surface token matrix pass.

Interpreted task:

- stop the page-by-page cleanup sequence for the moment
- build the shared surface-token foundation first
- use the existing global theme and surface architecture instead of creating a parallel theme system
- make future global theme work more centralized by defining explicit shared surface families and matrix recipes
