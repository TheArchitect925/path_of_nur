Continue with a focused content-fill phase for the real missing `kids_stories` and `quran_teacher` assets.

Constraints:
- Do not fabricate low-quality placeholders
- Keep Islamic educational tone gentle, respectful, and child-safe
- Separate `kids_stories` editorial artwork from `quran_teacher` visual-learning assets
- Prefer a generation-ready spec and prioritized batch order if live image generation is blocked by missing credentials

Priority:
1. `quran_teacher` placeholder + visual-learning pack
2. `kids_stories` covers and backdrops
3. `kids_stories` scene illustrations after the cover/backdrop batch
