# Prompt Archive

User request:

> for container descriptions, lets add a global theme for them and have them be put in a container thats 50 % Transparent glass and lets make it a shade darker then the rest of the containers to make them easier to distinguish. Do an audit of all the pages and apply those changes as a global change instead of page by page. The headers of the page can remain as is right now.
